# Force LF line endings for SQL migrations (prevents checksum mismatch between Windows/Linux)
backend/migrations/*.sql text eol=lf

# Shell scripts
*.sh text eol=lf
deploy/*.sh text eol=lf

# Docker files
Dockerfile text eol=lf
docker-compose*.yml text eol=lf
